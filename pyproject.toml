[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "code-watermarking"
version = "0.1.0"
description = "Watermarking utilities"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
